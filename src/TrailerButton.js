import Button from "@mui/material/Button";

function TrailerButton(){
  return(
    <Button
      variant="outlined"
      color="primary"
    >
      Watch Trailer!
    </Button>
  );
}

export default TrailerButton;
